class MainController < ApplicationController

  def dashboard
    # @trending_gifs = Gif.order_by(favorites.count).limit(5)
    # @trending_categories = Category.order_by(favorites.count).limit(5)
    # @popular_gifs = 
  end

end
